//when we have to print same statement for several times instead of copy paste we can use loop conditoon to print

#include <stdio.h>
int main(){
    for(int i=1;i <= 10;i+=1){
        printf("%d\n",i);
    }
    return 0;
}